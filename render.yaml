services:
  - type: web
    name: ai-chat-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: BING_API_KEY
        sync: false # Renderのダッシュボードで設定してください
